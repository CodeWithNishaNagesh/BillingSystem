name: AI Review

on:
  pull_request:

jobs:
  ai-review:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v4

    - name: Run AI Agent

      run: |
         dotnet run \
         --project Agents/ReviewAgent
